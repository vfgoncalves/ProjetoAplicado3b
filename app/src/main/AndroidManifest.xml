<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="projetoaplicado.vgoncalves.com.projetoaplicado">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_find_in_page"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_find_in_page"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".CadastrarActivity" />
        <activity android:name=".ContextoActivity" />
        <activity android:name=".CadastroEmpresaActivity" />
        <activity android:name=".MainEmpresaActivity"></activity>
    </application>

</manifest>